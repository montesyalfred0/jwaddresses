{
  "name": "jwaddresses",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ..",
    "dev": "cd backend && npm run dev & cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "docker:local": "docker compose -f docker-compose.local.yml up -d --build",
    "build:frontend": "cd frontend && npm run build",
    "deploy:vps": "cd frontend && npm run build && cp -r dist/* /var/www/jwaddresses/frontend/dist/"
  }
}
